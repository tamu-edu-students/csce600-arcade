<%= form_with(model: wordle) do |form| %>
  <% if wordle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wordle.errors.count, "error") %> prohibited this wordle from being saved:</h2>

      <ul>
        <% wordle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :play_date, style: "display: block" %>
    <%= form.text_field :play_date %>
  </div>

  <div>
    <%= form.label :word, style: "display: block" %>
    <%= form.text_field :word %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
